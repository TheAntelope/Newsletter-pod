__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
.env
.DS_Store
.feed_token.bin
.job_token.bin
.job_token.txt
.gcloud-proxy*.log
.uvicorn*.log
tmp-live-episode.mp3
sample_episode.mp3
welcome_v*.mp3
build/
DerivedData/
*.xcworkspace
*.xcodeproj/xcuserdata/
*.xcuserstate
ios/build/

# Design-tokens build artifacts. Generated outputs (DesignTokens.swift,
# dist/tokens.css) ARE committed so iOS/web builds don't need npm.
design-tokens/node_modules/

# Local Claude Code workspace: ignore screenshots / lock files / etc.,
# but keep settings.json under version control so project hooks travel
# with the repo. Use `.claude/*` (not `.claude/`) so the negation below
# can take effect — Git skips traversal entirely for an ignored dir.
.claude/*
!.claude/settings.json
