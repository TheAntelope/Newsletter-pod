{
  "name": "clawcast-design-tokens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for ClawCast design tokens (palette, spacing, radii). Generates Swift constants for the iOS app and CSS variables for the website.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  }
}
